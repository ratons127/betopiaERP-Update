[options]
; This is the password that allows database operations:
; admin_passwd = admin

; PostgreSQL connection (local)
db_host = localhost
db_port = 5432
db_user = betopia
db_password = 2wsx9IJN

; Addons path (repo-local)
addons_path = ./betopiaerp/addons,./addons

; Bind locally (Nginx will proxy)
http_interface = 127.0.0.1
http_port = 8069

; Longpolling/websocket
gevent_port = 8072

; Run in prefork mode so longpolling/websocket worker starts
workers = 2

; Behind Nginx reverse proxy
proxy_mode = True

default_productivity_apps = True
